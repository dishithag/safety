# scripts

One-off and code-generation programs (e.g. exporting enums, generating fixtures).
These are kept out of the production build graph. Run them explicitly with
`go run ./scripts/<name>` rather than wiring them into services.
